const Loader = () => {
    return (
        <div className="flex items-center justify-center w-full h-full bg-black/40 backdrop-blur-md">
        <span className="w-12 h-12 border-[5px] border-dotted
         border-white rounded-full animate-spin" />
        </div>
    );
};

export default Loader;